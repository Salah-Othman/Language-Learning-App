import 'package:flutter/material.dart';

class FamilyNembers extends StatelessWidget {
  const FamilyNembers({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('data')));
  }
}
